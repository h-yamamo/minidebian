#linux-image-5.XX.0-NN-amd64
  - you must install linux package in some way.

#firmware-iwlwifi
  - install the package if necessary,
    or manually copy required firmware files to /lib/firmware directory.
